# 评审报告：`2026-06-04-release-v1.9.0`

## 元数据

| 字段 | 值 |
|------|----|
| Reviewer | anvil-lead |
| MR / Commit | Release v1.9.0 |
| Author | caishilin |
| Review Date | 2026-06-04 |
| Status | `APPROVED` |

---

## 1. 自动化预检

| 检查项 | 命令 | 结果 | 备注 |
|--------|------|------|------|
| Build (Swift) | `xcodebuild build -scheme Shark` | PASS | Verified earlier in session |
| Lint (Swift) | `swiftformat` | N/A | Not installed in PATH |
| Lint (TS) | `tsc` | N/A | No TS project config, tsx runtime only |
| 运行时测试 | `build-tool create-dmg --no-build` | PASS | User-verified DMG created successfully |

---

## 2. 安全扫描

| 类别 | 发现 | 严重级别 | 状态 |
|------|------|----------|------|
| 硬编码密钥 | 无 | — | CLEAN |
| 注入风险 | `execSync` 用于构建脚本，输入来自 commander args（受控路径），无用户输入 | Low | ACCEPTED — 本地构建工具，无未受信输入 |
| 依赖 CVE | commander, @clack/prompts, tsx — 主流工具 | Low | ACCEPTED |
| npm OTP | OTP 可通过 `--otp` 传入命令行参数 | Low | 标准 npm 行为，无缓解措施 |

**安全结论：** CLEAN

---

## 3. Karpathy 对抗式原则

| 原则 | 结论 | 说明 |
|------|------|------|
| Think Before Coding | PASS | Workspace 分组需求明确，build-tool 替换 shell 脚本目的清晰 |
| Simplicity First | PASS | WorkspaceGroup 仅 18 行，build-tool.ts 将 shell 逻辑直译为 TS，无多余抽象 |
| Surgical Changes | PASS | 每行改动能追溯到需求：grouping → Swift, build-tool → TS, docs → version bump |
| Goal-Driven Execution | PASS | 改动可通过运行验证（build-tool create-dmg, xcodebuild） |

**Karpathy Score:** 4/4

---

## 4. 对抗式维度评审

### 4.1 设计：它是否应该存在？

| 行号 | 提问 | 评审判断 | 严重级别 |
|------|------|----------|----------|
| Workspace.swift:38-57 | WorkspaceGroup 是否必要？分组逻辑能否内联到 ListView？ | 独立 struct 更清晰，可测试性强 | PASS |
| build-tool.ts | TS 替代 shell 脚本是否值得？ | 是的——类型安全、依赖管理、clack UI 提升 DX | PASS |

**维度结论：** PASS

### 4.2 功能：作者遗漏了什么？

| 行号 | 提问 | 评审判断 | 严重级别 |
|------|------|----------|----------|
| WorkspaceListView.swift:194-212 | 分组后拖拽排序 `.onMove` 丢失，用户无法拖拽重排 | Low — 分组后按字母排序，同组内拖拽无意义，但 pinned 顺序定制能力减少 | INFO |
| WorkspaceGroup.groups() | 同名称不同项目会被错误归为一组 | Low — 当前工作流中用户手动控制名称，风险可控 | ACCEPTED |

**边界检查：**
- [x] 空 workspace 列表 → groups 返回空数组
- [x] 单 workspace → 一个 Section 一行
- [x] 搜索匹配 → 只显示匹配的 workspace（组内可能少一个类型）
- [x] 搜索无匹配 → 显示空结果视图

**维度结论：** PASS

### 4.3 复杂度：还能更简单吗？

| 行号 | 提问 | 评审判断 | 严重级别 |
|------|------|----------|----------|
| WorkspaceGroup.groups() | 拆分三行避免编译器类型检查超时 | 合理，非过度工程 | PASS |
| build-tool.ts | `capture` + `exec` 两个 helper 够用吗？ | 是的，职责清晰 | PASS |

**维度结论：** PASS

### 4.4 命名：名字是否撒谎？

| 行号 | 提问 | 评审判断 | 严重级别 |
|------|------|----------|----------|
| `WorkspaceGroup` | 名字准确描述功能 | PASS |
| `build-tool.ts` | 命名通用，含 create-dmg + publish 子命令 | PASS |

**维度结论：** PASS

### 4.5 注释：提供价值，还是替坏代码找借口？

注释简要解释 WHY，不赘述 WHAT。无 TODO。**PASS**

### 4.6 风格与一致性

Swift 代码遵循现有风格，TS 代码使用标准 Node.js 模式。**PASS**

### 4.7 上下文：系统是否更健康？

- 旧 shell 脚本已删除，构建流程统一到 TS ✓
- 新增 `node_modules/` 到 gitignore，不会误提交 ✓
- 文档同步更新 ✓

**维度结论：** PASS

### 4.8 测试

无自动化测试基础设施（Swift 项目无测试 target，TS 脚本为构建工具）。**N/A**

---

## 5. 发现项摘要

无 Critical/High 发现。

### Medium

| # | 维度 | 行号 | 描述 | 必须动作 |
|---|------|------|------|----------|
| 1 | 功能 | WorkspaceListView | 拖拽排序（.onMove）因分组移除。用户如需恢复，需在组级别重实现 | 非阻塞，记录为已知限制 |
| 2 | 安全 | build-tool.ts | `--otp` 参数可通过 `ps` 被看到。建议优先使用 `NPM_CONFIG_OTP` 环境变量 | 非阻塞，最佳实践建议 |

---

## 6. 门禁结论

| 门禁项 | 状态 |
|--------|------|
| 所有自动化检查通过 | [x] |
| 安全扫描干净 | [x] |
| Karpathy score = 4/4 | [x] |
| 无未解决 Critical 问题 | [x] |
| 无未解决 High 问题 | [x] |
| 评审文档完整 | [x] |

### 结论

- [ ] **BLOCK**
- [x] **APPROVE** — 所有门禁通过
